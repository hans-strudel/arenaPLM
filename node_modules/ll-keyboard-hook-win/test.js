const hook = require('./ll_keyboard_hooks');

hook.on('up', 'MediaPlayPause', () => {
  console.log('play pause up');
});

hook.on('down', 'MediaPlayPause', () => {
  console.log('play pause down');
});

hook.on('*', '*', (a) => console.log(a));

require('express')().listen(3000);
